# Project captures

Drop real interface captures here and reference them from `preview` in
`src/data/site.ts`. See ASSETS.md for what to capture and the rules.

The work scenes are composed to be complete WITHOUT these — an absent capture
gives a quieter plate built from the project's readings, never a broken image
and never a placeholder.
